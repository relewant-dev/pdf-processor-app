export default function LoginPage() {
  return (
    <main className="auth-shell">
      <section className="auth-card">
        <p className="eyebrow">Authentication</p>
        <h1>Login</h1>
        <p className="summary">Add the sign-in experience for Smart IDE users here.</p>
      </section>
    </main>
  );
}
